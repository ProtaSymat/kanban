<form class="bg-transparent" method="post" action="{{ path('app_board_delete', {'id': board.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ board.id) }}">
    <a><i class="fa fa-trash text-danger fs-3 ps-3"></i></a>
</form>
